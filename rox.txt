<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aromas del Hogar RL - Sahumerios y Aromatizantes</title>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600;700&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --color-primary: #6B2D5C;
            --color-secondary: #D4A574;
            --color-accent: #F4E4C1;
            --color-dark: #2C1810;
            --color-light: #FFF8F0;
            --gradient-bg: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #f5576c 75%, #f093fb 100%);
        }

        body {
            font-family: 'Lato', sans-serif;
            background: var(--color-light);
            color: var(--color-dark);
            line-height: 1.6;
        }

        /* Header */
        header {
            background: linear-gradient(135deg, #4a1c40 0%, #8B4513 50%, #D2691E 100%);
            color: white;
            padding: 1rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 20px rgba(0,0,0,0.3);
        }

        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .logo-icon {
            font-size: 2rem;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            position: relative;
        }

        nav a:hover {
            color: var(--color-accent);
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--color-accent);
            transition: width 0.3s;
        }

        nav a:hover::after {
            width: 100%;
        }

        .cart-icon {
            position: relative;
            cursor: pointer;
            font-size: 1.5rem;
            transition: transform 0.3s;
        }

        .cart-icon:hover {
            transform: scale(1.1);
        }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background: #ff6b6b;
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
            font-weight: bold;
        }

        /* Hero Section */
        .hero {
            margin-top: 80px;
            background: linear-gradient(135deg, rgba(107,45,92,0.9) 0%, rgba(139,69,19,0.8) 50%, rgba(210,105,30,0.9) 100%), 
                        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="40" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="0.5"/></svg>');
            background-size: cover;
            padding: 6rem 2rem;
            text-align: center;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200"><path d="M100,20 Q120,60 100,100 T100,180" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="2"><animate attributeName="d" values="M100,20 Q120,60 100,100 T100,180;M100,20 Q80,60 100,100 T100,180;M100,20 Q120,60 100,100 T100,180" dur="4s" repeatCount="indefinite"/></path></svg>');
            opacity: 0.3;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 800px;
            margin: 0 auto;
        }

        .hero h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3.5rem;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            animation: fadeInUp 1s ease;
        }

        .hero p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            opacity: 0.95;
            animation: fadeInUp 1s ease 0.2s both;
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(45deg, #25D366, #128C7E);
            color: white;
            padding: 1rem 2.5rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(37,211,102,0.4);
            animation: fadeInUp 1s ease 0.4s both;
            border: 2px solid transparent;
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(37,211,102,0.6);
            border-color: white;
        }

        /* Products Section */
        .products {
            padding: 4rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-title {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2.5rem;
            text-align: center;
            color: var(--color-primary);
            margin-bottom: 3rem;
            position: relative;
        }

        .section-title::after {
            content: '✨';
            display: block;
            font-size: 1.5rem;
            margin-top: 0.5rem;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .product-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: all 0.3s;
            position: relative;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(107,45,92,0.2);
        }

        .product-image {
            height: 250px;
            background: linear-gradient(135deg, #f5f5f5 0%, #e0e0e0 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 4rem;
            position: relative;
            overflow: hidden;
        }

        .product-image::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.3) 0%, transparent 70%);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .product-info {
            padding: 1.5rem;
        }

        .product-category {
            color: var(--color-secondary);
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: bold;
        }

        .product-name {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            color: var(--color-primary);
            margin: 0.5rem 0;
        }

        .product-description {
            color: #666;
            font-size: 0.95rem;
            margin-bottom: 1rem;
        }

        .product-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .product-price {
            font-size: 1.4rem;
            font-weight: bold;
            color: var(--color-primary);
        }

        .add-to-cart {
            background: var(--color-primary);
            color: white;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: 25px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .add-to-cart:hover {
            background: var(--color-secondary);
            transform: scale(1.05);
        }

        /* Cart Sidebar */
        .cart-sidebar {
            position: fixed;
            top: 0;
            right: -400px;
            width: 400px;
            height: 100%;
            background: white;
            box-shadow: -5px 0 30px rgba(0,0,0,0.2);
            z-index: 2000;
            transition: right 0.3s;
            display: flex;
            flex-direction: column;
        }

        .cart-sidebar.open {
            right: 0;
        }

        .cart-header {
            background: var(--color-primary);
            color: white;
            padding: 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .close-cart {
            background: none;
            border: none;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
        }

        .cart-items {
            flex: 1;
            overflow-y: auto;
            padding: 1rem;
        }

        .cart-item {
            display: flex;
            gap: 1rem;
            padding: 1rem;
            border-bottom: 1px solid #eee;
            align-items: center;
        }

        .cart-item-image {
            width: 60px;
            height: 60px;
            background: #f0f0f0;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
        }

        .cart-item-details {
            flex: 1;
        }

        .cart-item-name {
            font-weight: bold;
            color: var(--color-primary);
        }

        .cart-item-price {
            color: var(--color-secondary);
        }

        .remove-item {
            background: #ff6b6b;
            color: white;
            border: none;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            cursor: pointer;
            transition: all 0.3s;
        }

        .remove-item:hover {
            background: #ff5252;
            transform: scale(1.1);
        }

        .cart-footer {
            padding: 1.5rem;
            border-top: 2px solid #eee;
            background: #f9f9f9;
        }

        .cart-total {
            font-size: 1.3rem;
            font-weight: bold;
            color: var(--color-primary);
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
        }

        .whatsapp-checkout {
            display: block;
            width: 100%;
            background: linear-gradient(45deg, #25D366, #128C7E);
            color: white;
            text-align: center;
            padding: 1rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            transition: all 0.3s;
            margin-bottom: 0.5rem;
        }

        .whatsapp-checkout:hover {
            transform: scale(1.02);
            box-shadow: 0 5px 15px rgba(37,211,102,0.4);
        }

        .clear-cart {
            width: 100%;
            background: transparent;
            color: #666;
            border: 2px solid #ddd;
            padding: 0.8rem;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .clear-cart:hover {
            border-color: #ff6b6b;
            color: #ff6b6b;
        }

        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 1500;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
        }

        .overlay.active {
            opacity: 1;
            visibility: visible;
        }

        /* About Section */
        .about {
            background: linear-gradient(135deg, #f8f4f0 0%, #fff 100%);
            padding: 4rem 2rem;
            text-align: center;
        }

        .about-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .about h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2.5rem;
            color: var(--color-primary);
            margin-bottom: 1.5rem;
        }

        .about p {
            font-size: 1.1rem;
            color: #555;
            line-height: 1.8;
        }

        /* Footer */
        footer {
            background: var(--color-dark);
            color: white;
            padding: 3rem 2rem 1rem;
            text-align: center;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 2rem;
            text-align: left;
        }

        .footer-section h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--color-accent);
        }

        .footer-section p, .footer-section a {
            color: #ccc;
            text-decoration: none;
            line-height: 1.8;
        }

        .footer-section a:hover {
            color: var(--color-accent);
        }

        .social-links {
            display: flex;
            gap: 1rem;
            margin-top: 1rem;
        }

        .social-links a {
            width: 40px;
            height: 40px;
            background: rgba(255,255,255,0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }

        .social-links a:hover {
            background: var(--color-secondary);
            transform: translateY(-3px);
        }

        .footer-bottom {
            border-top: 1px solid rgba(255,255,255,0.1);
            padding-top: 2rem;
            color: #888;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Floating WhatsApp Button */
        .float-whatsapp {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 60px;
            height: 60px;
            background: #25D366;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            box-shadow: 0 4px 20px rgba(37,211,102,0.4);
            z-index: 1000;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            animation: pulse 2s infinite;
        }

        .float-whatsapp:hover {
            transform: scale(1.1);
            box-shadow: 0 6px 30px rgba(37,211,102,0.6);
        }

        @keyframes pulse {
            0%, 100% { box-shadow: 0 4px 20px rgba(37,211,102,0.4); }
            50% { box-shadow: 0 4px 30px rgba(37,211,102,0.7); }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
            }

            nav ul {
                gap: 1rem;
                font-size: 0.9rem;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .cart-sidebar {
                width: 100%;
                right: -100%;
            }

            .products-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header>
        <div class="header-content">
            <div class="logo">
                <span class="logo-icon">🕯️</span>
                <span>Aromas del Hogar RL</span>
            </div>
            <nav>
                <ul>
                    <li><a href="#inicio">Inicio</a></li>
                    <li><a href="#productos">Productos</a></li>
                    <li><a href="#nosotros">Nosotros</a></li>
                    <li><a href="#contacto">Contacto</a></li>
                </ul>
            </nav>
            <div class="cart-icon" onclick="toggleCart()">
                🛒
                <span class="cart-count" id="cartCount">0</span>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="inicio">
        <div class="hero-content">
            <h1>Aromas que Transforman tu Hogar</h1>
            <p>Descubrí nuestra colección de sahumerios, esencias y aromatizantes para llenar tus espacios de armonía, bienestar y energía positiva.</p>
            <a href="https://wa.me/5491130321323?text=¡Hola!%20Me%20interesan%20los%20productos%20de%20Aromas%20del%20Hogar%20RL" class="cta-button" target="_blank">
                💬 Escribinos por WhatsApp
            </a>
        </div>
    </section>

    <!-- Products Section -->
    <section class="products" id="productos">
        <h2 class="section-title">Nuestros Productos</h2>
        <div class="products-grid" id="productsGrid">
            <!-- Products will be inserted here by JavaScript -->
        </div>
    </section>

    <!-- About Section -->
    <section class="about" id="nosotros">
        <div class="about-content">
            <h2>Sobre Nosotros</h2>
            <p>En <strong>Aromas del Hogar RL</strong> creemos que los aromas tienen el poder de transformar ambientes y estados de ánimo. Ofrecemos sahumerios artesanales, hornitos, esencias naturales, aromatizantes de ambiente y telas, aromatizantes para autos y productos de limpieza energética. Cada producto está seleccionado para brindarte armonía, bienestar y energía positiva. Aromas que acompañan, envuelven y transforman tu hogar.</p>
        </div>
    </section>

    <!-- Cart Sidebar -->
    <div class="overlay" id="overlay" onclick="toggleCart()"></div>
    <div class="cart-sidebar" id="cartSidebar">
        <div class="cart-header">
            <h3>Tu Carrito 🛒</h3>
            <button class="close-cart" onclick="toggleCart()">✕</button>
        </div>
        <div class="cart-items" id="cartItems">
            <!-- Cart items will be inserted here -->
        </div>
        <div class="cart-footer">
            <div class="cart-total">
                <span>Total:</span>
                <span id="cartTotal">$0</span>
            </div>
            <a href="#" class="whatsapp-checkout" id="whatsappCheckout" onclick="sendWhatsAppOrder()">
                📱 Comprar por WhatsApp
            </a>
            <button class="clear-cart" onclick="clearCart()">Vaciar Carrito</button>
        </div>
    </div>

    <!-- Floating WhatsApp -->
    <a href="https://wa.me/5491130321323" class="float-whatsapp" target="_blank" title="Chateá con nosotros">
        💬
    </a>

    <!-- Footer -->
    <footer id="contacto">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Aromas del Hogar RL</h3>
                <p>Tu espacio de bienestar y armonía. Productos seleccionados con amor para transformar tus ambientes.</p>
            </div>
            <div class="footer-section">
                <h3>Contacto</h3>
                <p>📱 WhatsApp: +54 9 11 3032-1323</p>
                <p>📍 Buenos Aires, Argentina</p>
                <p>✉️ aromasdelhogar@email.com</p>
            </div>
            <div class="footer-section">
                <h3>Seguinos</h3>
                <div class="social-links">
                    <a href="#">📘</a>
                    <a href="#">📸</a>
                    <a href="#">💬</a>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2025 Aromas del Hogar RL. Todos los derechos reservados. 🕯️</p>
        </div>
    </footer>

    <script>
        // Product Data
        const products = [
            {
                id: 1,
                name: "Sahumerios Artesanales",
                category: "Sahumerios",
                description: "Variedad de aromas naturales para purificar y aromatizar tus espacios.",
                price: 1500,
                emoji: "🌿"
            },
            {
                id: 2,
                name: "Hornitos de Cerámica",
                category: "Hornitos",
                description: "Diseños únicos para quemar esencias y crear ambientes relajantes.",
                price: 4500,
                emoji: "🏺"
            },
            {
                id: 3,
                name: "Esencias Naturales",
                category: "Esencias",
                description: "Aceites esenciales puros para aromaterapia y bienestar.",
                price: 2800,
                emoji: "💧"
            },
            {
                id: 4,
                name: "Aromatizante de Ambiente",
                category: "Aromatizantes",
                description: "Fragancias duraderas para tu hogar u oficina.",
                price: 2200,
                emoji: "🌸"
            },
            {
                id: 5,
                name: "Aromatizante para Autos",
                category: "Aromatizantes",
                description: "Mantené tu auto siempre fresco y perfumado.",
                price: 1200,
                emoji: "🚗"
            },
            {
                id: 6,
                name: "Limpieza Energética",
                category: "Bienestar",
                description: "Sahumerios y resinas para limpieza energética del hogar.",
                price: 3500,
                emoji: "✨"
            },
            {
                id: 7,
                name: "Velas Aromáticas",
                category: "Velas",
                description: "Velas de soja con aceites esenciales naturales.",
                price: 3200,
                emoji: "🕯️"
            },
            {
                id: 8,
                name: "Kit de Bienestar",
                category: "Kits",
                description: "Combinación de sahumerios, esencias y hornito especial.",
                price: 8900,
                emoji: "🎁"
            }
        ];

        let cart = [];

        // Render Products
        function renderProducts() {
            const grid = document.getElementById('productsGrid');
            grid.innerHTML = products.map(product => `
                <div class="product-card">
                    <div class="product-image">${product.emoji}</div>
                    <div class="product-info">
                        <div class="product-category">${product.category}</div>
                        <h3 class="product-name">${product.name}</h3>
                        <p class="product-description">${product.description}</p>
                        <div class="product-footer">
                            <span class="product-price">$${product.price.toLocaleString()}</span>
                            <button class="add-to-cart" onclick="addToCart(${product.id})">
                                <span>+</span> Agregar
                            </button>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        // Cart Functions
        function addToCart(productId) {
            const product = products.find(p => p.id === productId);
            const existingItem = cart.find(item => item.id === productId);
            
            if (existingItem) {
                existingItem.quantity++;
            } else {
                cart.push({ ...product, quantity: 1 });
            }
            
            updateCart();
            toggleCart();
        }

        function removeFromCart(productId) {
            cart = cart.filter(item => item.id !== productId);
            updateCart();
        }

        function clearCart() {
            cart = [];
            updateCart();
        }

        function updateCart() {
            const cartCount = document.getElementById('cartCount');
            const cartItems = document.getElementById('cartItems');
            const cartTotal = document.getElementById('cartTotal');
            
            const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
            const totalPrice = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
            
            cartCount.textContent = totalItems;
            cartTotal.textContent = '$' + totalPrice.toLocaleString();
            
            if (cart.length === 0) {
                cartItems.innerHTML = '<p style="text-align: center; color: #999; padding: 2rem;">Tu carrito está vacío 🛒</p>';
            } else {
                cartItems.innerHTML = cart.map(item => `
                    <div class="cart-item">
                        <div class="cart-item-image">${item.emoji}</div>
                        <div class="cart-item-details">
                            <div class="cart-item-name">${item.name}</div>
                            <div class="cart-item-price">$${item.price.toLocaleString()} x ${item.quantity}</div>
                        </div>
                        <button class="remove-item" onclick="removeFromCart(${item.id})">✕</button>
                    </div>
                `).join('');
            }
        }

        function toggleCart() {
            const sidebar = document.getElementById('cartSidebar');
            const overlay = document.getElementById('overlay');
            sidebar.classList.toggle('open');
            overlay.classList.toggle('active');
        }

        function sendWhatsAppOrder() {
            if (cart.length === 0) {
                alert('Tu carrito está vacío. Agregá productos primero.');
                return;
            }
            
            let message = '¡Hola! Quiero hacer un pedido de Aromas del Hogar RL:%0A%0A';
            let total = 0;
            
            cart.forEach(item => {
                message += • ${item.name} x${item.quantity} - $${(item.price * item.quantity).toLocaleString()}%0A;
                total += item.price * item.quantity;
            });
            
            message += %0ATotal: $${total.toLocaleString()}%0A%0AGracias!;
            
            const whatsappUrl = https://wa.me/5491130321323?text=${message};
            window.open(whatsappUrl, '_blank');
        }

        // Initialize
        renderProducts();
        updateCart();
    </script>

</body>
</html>

